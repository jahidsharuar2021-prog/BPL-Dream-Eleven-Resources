import React from 'react';

const Selected = ({ selectedPlayers }) => {
  console.log(selectedPlayers);
  return (
    <div>
      <h2> None akono work running</h2>
    </div>
  );
};

export default Selected;